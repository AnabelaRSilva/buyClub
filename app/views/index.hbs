<div class="section">
  <div class="section-header">
    <h1>{{title }}</h1>
  </div>
  <div class="section-body">
    <p>Welcome to {{title }}</p>
    <ul class="people_list">
      {{#each users}}
      <li>{{this.email}} {{this.password}}</li>
      {{/each}}
    </ul>
  </div>
</div>